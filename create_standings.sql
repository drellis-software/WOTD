CREATE TABLE standings (
Name TEXT,
D DATE,
W INTEGER,
L INTEGER,
T INTEGER,
P INTEGER
);

CREATE TABLE weddle (
Name TEXT,
day DATE,
W INTEGER,
L INTEGER,
T INTEGER,
G INTEGER
);

CREATE TABLE pickle (
Name TEXT,
day DATE,
W INTEGER,
L INTEGER,
T INTEGER,
G INTEGER
);

CREATE TABLE hard (
Name TEXT,
day DATE,
W INTEGER,
L INTEGER,
T INTEGER,
G INTEGER
);